<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for geofencing on Android 10+ (API 29+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Pomodoro timer notifications and exact alarms -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />
    <!-- Storage permissions for PDF export (Android 9 and below) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Anchornotes_team3">
        
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        
        <!-- Login-only Activity - First screen users see -->
        <activity
            android:name=".LoginOnlyActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity - Registration -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Main Activity - Homepage -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Account Activity -->
        <activity
            android:name=".AccountActivity"
            android:exported="false" />

        <!-- Note Editor Activity -->
        <activity
            android:name=".NoteEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Search Results Activity -->
        <activity
            android:name=".SearchResultsActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Filter Options Activity -->
        <activity
            android:name=".FilterOptionsActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Filter Results Activity -->
        <activity
            android:name=".FilterResultsActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Template Activity -->
        <activity
            android:name=".TemplateActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Statistics Activity -->
        <activity
            android:name=".StatisticsActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Pomodoro Activity -->
        <activity
            android:name=".PomodoroActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Map Location Picker Activity -->
        <activity
            android:name=".MapLocationPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- Drawing Activity -->
        <activity
            android:name=".DrawingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Anchornotes_team3" />

        <!-- FileProvider for sharing files with camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Geofence BroadcastReceiver -->
        <receiver
            android:name=".geofence.GeofenceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.anchornotes_team3.GEOFENCE_EVENT" />
            </intent-filter>
        </receiver>

        <!-- Pomodoro completion receiver -->
        <receiver
            android:name=".timer.PomodoroReceiver"
            android:exported="false" />
    </application>

</manifest>