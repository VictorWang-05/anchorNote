# Database Configuration (Optional - only if you need custom tables)
# spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://127.0.0.1:54322/postgres}
# spring.datasource.username=${SUPABASE_DB_USER:postgres}
# spring.datasource.password=${SUPABASE_DB_PASSWORD:postgres}
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration (Disabled - using Supabase Auth only)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.jdbc.time_zone=UTC
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.com.csci310.anchornotes=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG


